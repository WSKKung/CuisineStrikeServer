export enum EndResultStringKeys {
	GENERIC = "generic",
	HP_REACHES_ZERO = "hp_reaches_zero",
	DECKED_OUT = "decked_out",
	TIMEOUT = "timeout",
	DISCONNECTED = "disconnected",
	SURRENDER = "surrender"
}

export enum SelectionHintStringKeys {
	GENERIC = "select.generic",
	CARDS = "select.cards",
	ZONES = "select.zones",
	YES_NO = "select.yes_no",
	OPTION = "select.option"
}